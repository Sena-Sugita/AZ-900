# 3. Azure の管理とガバナンス

> この章では、Azure のコスト管理、ガバナンス、デプロイツール、監視ツールについて学びます。
> AZ-900 試験全体の **30～35%** を占める重要な分野です。

---

## 3.1 コスト管理

### Azure のコストに影響する要因

Azure の料金は「使った分だけ」支払う従量課金が基本ですが、**何がコストに影響するのか**を理解しておくことが重要です。

| 要因 | 説明 | 例え |
|:---|:---|:---|
| **リソースの種類** | VM、ストレージ、データベースなど種類によって単価が異なる | レストランのメニューの値段が料理ごとに違うのと同じ |
| **リソースのサイズ** | CPU やメモリが多いほど高い | 軽自動車より大型車のレンタル料金が高いのと同じ |
| **使用量** | 稼働時間やデータ転送量に応じて課金 | 電気代が使用量で変わるのと同じ |
| **リージョン** | リージョンによって同じサービスでも価格が異なる | 都心と地方で家賃が違うのと同じ |
| **ネットワークトラフィック** | Azure からの**送信（エグレス）** データに課金（受信は無料） | 宅配便の送料のようなもの。届くのは無料だが発送にはお金がかかる |

#### 身近な例えで理解しよう

Azure のコストは**スマートフォンの料金プラン**に似ています。機種（リソースの種類）、プラン（サイズ）、通話やデータ使用量、エリア（リージョン）によって毎月の料金が変わります。

> **試験対策ポイント**: 「Azure への受信データ（イングレス）は無料、送信データ（エグレス）は有料」は頻出です。また「同じサービスでもリージョンによって価格が異なる」ことも問われます。

---

### 料金計算ツール（Azure Pricing Calculator）

Azure Pricing Calculator は、**Azure サービスの月額料金を事前に見積もるためのツール**です。

#### 身近な例えで理解しよう

家を建てる前の**建築費用の見積書**のようなものです。「こういう間取りで、こういう素材を使うと、いくらかかりますか？」という計算を、Azure サービスに対して行います。

#### 特徴
- **Azure を使い始める前**に利用するツール
- リソースの種類、リージョン、使用量などを入力して見積もりを作成
- 見積もりを保存・エクスポート・共有できる
- **無料**で使える Web ベースのツール

> **試験対策ポイント**: 「Azure に移行する前にコストを見積もりたい」→ **Azure Pricing Calculator** です。「移行前の見積もり」がキーワードです。

---

### 総所有コスト（TCO）計算ツール

TCO（Total Cost of Ownership）計算ツールは、**オンプレミス環境を Azure に移行した場合のコスト削減効果を比較するツール**です。

#### 身近な例えで理解しよう

**自家用車 vs カーシェアリングの比較シミュレーション**のようなものです。「車を持つと購入費 + 保険 + 駐車場代 + 車検代で年間 ○○ 万円。カーシェアだと年間 △△ 万円。カーシェアの方が □□ 万円お得！」と計算してくれます。

#### 特徴
- **オンプレミスの現在のコスト** と **Azure に移行した場合のコスト** を比較
- サーバー、ストレージ、ネットワーク、人件費、電力費などを含めた**総コスト**を算出
- 経営層への提案資料作成に最適

#### Pricing Calculator と TCO Calculator の違い

| ツール | 目的 | いつ使う？ |
|:---|:---|:---|
| **Pricing Calculator** | Azure サービスの料金を見積もる | Azure で使うサービスのコストを知りたい時 |
| **TCO Calculator** | オンプレミスと Azure のコストを比較する | Azure への移行を検討している時 |

> **試験対策ポイント**: 「オンプレミスと Azure のコスト比較」→ **TCO Calculator**。「Azure サービスの料金見積もり」→ **Pricing Calculator**。この 2 つの使い分けは非常によく問われます。

---

### Azure Cost Management and Billing

Azure Cost Management and Billing は、**Azure の利用コストをリアルタイムで監視・分析・最適化するためのサービス**です。

#### 身近な例えで理解しよう

スマートフォンの**料金明細アプリ**のようなものです。「今月はデータ通信に ○○ 円使った」「先月より △△ 円増えた」「このまま使い続けると月末には □□ 円になりそう」と教えてくれます。

#### 主な機能

| 機能 | 説明 |
|:---|:---|
| **コスト分析** | どのサービスにいくらかかっているかを視覚的に表示 |
| **予算** | 月額の上限を設定し、超えそうになったらアラートを送信 |
| **推奨事項** | コスト削減のためのアドバイスを提供 |
| **請求書** | 請求書の確認とダウンロード |

> **試験対策ポイント**: 「現在の Azure のコストを監視・分析したい」→ **Azure Cost Management and Billing** です。「予算アラートを設定したい」場合もこのサービスを使います。Pricing Calculator（見積もり）とは違い、**実際に発生しているコスト**を管理するツールです。

---

### タグ（Tags）によるコスト管理

タグとは、**Azure リソースに付けることができる「名前：値」のペアのラベル**です。

#### 身近な例えで理解しよう

オフィスの備品に**管理シール**を貼るようなものです。「部署：営業部」「プロジェクト：新商品開発」「環境：本番」のようにシールを貼っておけば、後から「営業部の備品は全部でいくら？」と集計できます。

#### タグの使い方の例

| タグ名 | 値の例 | 用途 |
|:---|:---|:---|
| Department | Sales, Engineering | 部門別コスト集計 |
| Environment | Production, Development | 環境別の管理 |
| Project | ProjectA, ProjectB | プロジェクト別コスト追跡 |
| Owner | tanaka@example.com | 責任者の特定 |

#### タグの注意点
- タグは**自動的に継承されない**（リソースグループにタグを付けても、中のリソースには付かない）
- タグの付与を強制するには **Azure Policy** を使う
- タグはコスト管理だけでなく、リソースの整理や自動化にも使える

> **試験対策ポイント**: 「タグはリソースグループから子リソースに自動継承されるか？」→ **されない** が正解です。「タグの付与を強制するには？」→ **Azure Policy** です。この 2 つは頻出です。

---

### Azure のコスト最適化の目的（Azure Reservations / Azure Spot VM など）

Azure にはコストを削減するためのいくつかの仕組みがあります。

| 方法 | 概要 | 例え | 割引率の目安 |
|:---|:---|:---|:---|
| **Azure Reservations** | 1 年または 3 年の長期利用を予約して割引を受ける | ジムの**年間会員**（月払いより安い） | 最大 72% |
| **Azure Spot VM** | Azure の余剰キャパシティを格安で利用（ただし突然停止される可能性あり） | 航空券の**スタンバイ席**（空いていれば格安、満席なら乗れない） | 最大 90% |
| **Azure Hybrid Benefit** | 既存のオンプレミスライセンスを Azure で再利用 | 引っ越し先でも**前の家の家具を使い回す** | 最大 85% |
| **リソースのサイズ変更** | 使っていない余剰リソースを適切なサイズに変更 | 一人暮らしなのに**3LDK を借りているなら 1K に引っ越す** | ケースバイケース |

> **試験対策ポイント**: 「長期利用でコスト削減」→ **Azure Reservations**。「余剰キャパシティで格安だが中断される可能性」→ **Azure Spot VM**。「オンプレミスのライセンスをクラウドで再利用」→ **Azure Hybrid Benefit** です。

---

## 3.2 Azure のガバナンスとコンプライアンス機能

### Azure Blueprints

Azure Blueprints は、**Azure 環境を標準化されたテンプレートとして定義し、繰り返しデプロイできるサービス**です。

#### 身近な例えで理解しよう

**建売住宅の設計図（ブループリント）** を想像してください。間取り、配線、水道管の配置がすべて設計図に書かれていて、同じ設計図から何軒でも同じ品質の家を建てられます。Azure Blueprints も同じで、ポリシー、RBAC、ARM テンプレート、リソースグループなどを 1 つのパッケージにまとめて、何度でも同じ環境をデプロイできます。

#### Blueprints に含められるもの（成果物）
- **ロールの割り当て**（RBAC）
- **ポリシーの割り当て**（Azure Policy）
- **ARM テンプレート**
- **リソースグループ**

#### 特徴
- ブループリントと実際のデプロイの**関係が追跡**される（何をデプロイしたか記録が残る）
- **バージョン管理**ができる
- 組織全体で標準化された環境を素早く展開できる

> **試験対策ポイント**: 「環境のセットアップを標準化し、繰り返しデプロイしたい」→ **Azure Blueprints** です。ARM テンプレート単体との違いは、Blueprints は**ポリシーや RBAC も含めた一括パッケージ**であり、デプロイとの関係が追跡される点です。

---

### Azure Policy

Azure Policy は、**Azure リソースに対するルール（ポリシー）を作成・割り当て・管理するサービス**です。ルールに違反するリソースの作成を防止したり、既存のリソースのコンプライアンスを評価したりします。

#### 身近な例えで理解しよう

会社の**就業規則**のようなものです。「全社員はスーツを着用すること」「会議室の予約は 2 時間まで」のようにルールを定め、違反した場合は注意する（監査）か、そもそも違反できないようにする（拒否）ことができます。

#### Azure Policy の主な動作

| 効果（Effect） | 動作 |
|:---|:---|
| **Deny** | ルールに違反するリソースの作成・変更を**拒否** |
| **Audit** | 違反するリソースを**記録**するが、作成は許可 |
| **Append** | リソース作成時に**設定を自動追加** |
| **DeployIfNotExists** | 条件に合致するリソースが存在しない場合に**自動デプロイ** |

#### イニシアティブとは

**イニシアティブ**は、複数の関連するポリシーを 1 つのグループにまとめたものです。

例え: 就業規則の中の「服装規定」という**カテゴリ**のようなもの。「スーツ着用」「ネクタイ着用」「名札着用」など複数のルールがまとまっています。

> **試験対策ポイント**: 「特定のリージョンにのみリソースの作成を許可したい」→ **Azure Policy** です。「複数のポリシーをまとめたものは？」→ **イニシアティブ** です。Policy は「ルールの強制」、RBAC は「人の権限管理」という違いを明確にしましょう。

---

### リソースロック（Resource Locks）

リソースロックは、**Azure リソースの誤削除や誤変更を防ぐための保護機能**です。

#### 身近な例えで理解しよう

大事な書類に**「重要！捨てないで！」のステッカー**を貼るようなものです。さらに強力な保護として、書類を**透明なケースに入れて鍵をかける**（読み取りはできるが、書き換えも削除もできない）こともできます。

#### リソースロックの種類

| ロックの種類 | できること | できないこと |
|:---|:---|:---|
| **削除ロック（Delete）** | リソースの読み取り、変更 | リソースの**削除** |
| **読み取り専用ロック（ReadOnly）** | リソースの**読み取りのみ** | リソースの**変更と削除** |

#### 重要なポイント
- ロックは**継承**される（リソースグループにロックを設定すると、中のリソースにも適用）
- ロックは**すべてのユーザー**に適用される（所有者でもロックを解除しないと変更・削除できない）
- ロックを解除してから変更・削除する手順が必要

#### Azure Policy / RBAC / リソースロックの比較

| 機能 | 目的 | 対象 | 例 |
|:---|:---|:---|:---|
| **Azure Policy** | リソースの**ルール**を強制する | リソースの構成 | 「東日本リージョンにのみ VM を作成可能」 |
| **RBAC** | **誰が何をできるか**を制御する | ユーザーの権限 | 「田中さんは VM を作成できるが、削除はできない」 |
| **リソースロック** | リソースの**誤操作を防止**する | リソース全体 | 「この VM は誰であっても削除できない」 |

> **試験対策ポイント**: 「本番環境のリソースを誤って削除されないようにしたい」→ **リソースロック（削除ロック）** です。Policy・RBAC・リソースロックの 3 つの違いは最頻出です。「リソースロックは所有者にも適用される」という点も問われます。

---

### Microsoft Purview（Service Trust Portal）

#### Microsoft Purview

Microsoft Purview は、**組織全体のデータを発見・分類・管理するための統合データガバナンスサービス**です。

#### 身近な例えで理解しよう

大企業の**資料室の司書**のようなものです。会社中に散らばっている書類を見つけ出し、「これは機密」「これは公開可」とラベルを貼って整理し、誰がどの書類を見たか記録してくれます。

#### 主な機能
- **データカタログ**: 組織全体のデータ資産を発見・管理
- **データ分類**: 機密データの自動検出とラベル付け
- **コンプライアンス管理**: データ保護の規制への対応

#### Service Trust Portal

Service Trust Portal は、**Microsoft のセキュリティ、プライバシー、コンプライアンスに関する情報を提供するポータル**です。

- 監査レポート
- コンプライアンスドキュメント
- セキュリティ評価レポート

例え: 食品メーカーの**品質検査報告書を閲覧できる窓口**のようなもの。「この製品は安全基準を満たしています」という証明書を確認できます。

> **試験対策ポイント**: 「Microsoft のコンプライアンスレポートを確認したい」→ **Service Trust Portal** です。「組織のデータを統合的に管理・分類したい」→ **Microsoft Purview** です。

---

### Azure のコンプライアンスとデータ保護の標準

Azure は世界中のさまざまな**コンプライアンス基準（規制や業界標準）** に準拠しています。

| 基準 | 概要 |
|:---|:---|
| **GDPR** | EU の個人データ保護規則 |
| **ISO 27001** | 情報セキュリティマネジメントの国際標準 |
| **SOC 1 / SOC 2** | サービス組織の内部統制に関する報告書 |
| **NIST** | 米国のサイバーセキュリティフレームワーク |
| **FedRAMP** | 米国政府のクラウドセキュリティ基準 |

#### ポイント
- Azure は **90 以上** のコンプライアンス認証を取得
- コンプライアンスの対象はリージョンや業界によって異なる
- **Azure のコンプライアンス ≠ あなたの組織のコンプライアンス**（Azure が基準を満たしていても、ユーザー側の責任は残る → 共有責任モデル）

> **試験対策ポイント**: 「Azure がコンプライアンス基準に準拠していれば、顧客もコンプライアンスに準拠していると言えるか？」→ **言えない**。共有責任モデルにより、データの管理やアクセス制御はユーザーの責任です。

---

## 3.3 Azure リソースの管理とデプロイのためのツール

### Azure Portal

Azure Portal は、**Web ブラウザから Azure のすべてのサービスを管理できる GUI（グラフィカルユーザーインターフェース）** です。

#### 身近な例えで理解しよう

Azure の**コントロールパネル（管理画面）** です。スマートフォンのホーム画面のように、アイコンやメニューをクリックするだけで、VM の作成やストレージの設定など、あらゆる操作ができます。

#### 特徴
- Web ブラウザがあればどこからでもアクセス可能
- 視覚的で直感的な操作が可能
- ダッシュボードをカスタマイズして、リソースの状態を一覧表示
- **常に最新**（Azure の新機能は Portal にすぐ反映される）

> **試験対策ポイント**: Azure Portal は「GUI ベース」の管理ツールとして覚えましょう。自動化には向きませんが、初心者や視覚的な操作に最適です。

---

### Azure Cloud Shell（Bash / PowerShell）

Azure Cloud Shell は、**Azure Portal 内からブラウザ上で利用できるコマンドライン環境**です。

#### 身近な例えで理解しよう

Azure Portal の中に組み込まれた**ミニ端末**のようなものです。自分のパソコンに何もインストールしなくても、ブラウザ上でコマンドを実行できます。

#### 特徴
- **Bash** と **PowerShell** の 2 つのシェルを選択可能
- Azure CLI と Azure PowerShell が**あらかじめインストール済み**
- ブラウザさえあればどの端末からでも利用可能
- ファイルの保存には **Azure Storage アカウント**が必要（初回使用時に作成）

> **試験対策ポイント**: 「ソフトをインストールせずにブラウザからコマンドライン操作をしたい」→ **Azure Cloud Shell** です。Bash と PowerShell の両方が使えることもポイントです。

---

### Azure CLI

Azure CLI は、**コマンドライン（ターミナル）から Azure リソースを管理するためのツール**です。

#### 特徴
- **Bash スタイル**のコマンド構文
- Windows、macOS、Linux にインストール可能
- スクリプトによる**自動化**に最適
- コマンド例：`az vm create --name MyVM --resource-group MyRG`

> **試験対策ポイント**: Azure CLI のコマンドは `az` で始まります。

---

### Azure PowerShell

Azure PowerShell は、**PowerShell 環境から Azure リソースを管理するためのモジュール**です。

#### 特徴
- **PowerShell スタイル**のコマンド構文（動詞-名詞 形式）
- Windows、macOS、Linux にインストール可能
- コマンド例：`New-AzVm -Name MyVM -ResourceGroupName MyRG`

#### Azure CLI と Azure PowerShell の比較

| 項目 | Azure CLI | Azure PowerShell |
|:---|:---|:---|
| **コマンド構文** | `az <サービス> <アクション>` | `動詞-Az名詞` |
| **例** | `az vm list` | `Get-AzVM` |
| **得意な人** | Linux / Bash に慣れている人 | Windows / PowerShell に慣れている人 |
| **できること** | 同じ | 同じ |

**ポイント**: Azure CLI と Azure PowerShell は**できることは同じ**です。構文が違うだけで、どちらでも同じ操作ができます。

> **試験対策ポイント**: 「`az` コマンドはどのツール？」→ **Azure CLI**。「`Get-AzVM` のようなコマンドはどのツール？」→ **Azure PowerShell**。コマンドの形式から判別する問題が出ます。

---

### Azure Arc

Azure Arc は、**Azure の管理機能を Azure 以外の環境（オンプレミス、他のクラウド）にも拡張するサービス**です。

#### 身近な例えで理解しよう

**リモコンの範囲を拡大する中継器**のようなものです。普通は Azure のリモコン（管理ツール）は Azure 内のリソースしか操作できませんが、Arc を使うと、自社サーバーや AWS・GCP のリソースまで Azure Portal から一元管理できるようになります。

#### Azure Arc で管理できるもの
- オンプレミスのサーバー
- 他のクラウド（AWS、GCP）のサーバー
- Kubernetes クラスター
- SQL Server

> **試験対策ポイント**: 「Azure 以外の環境を Azure Portal から一元管理したい」→ **Azure Arc** です。「マルチクラウド管理」「ハイブリッド管理」のキーワードと結びつけましょう。

---

### Azure Resource Manager（ARM）

Azure Resource Manager（ARM）は、**Azure のすべてのリソース管理操作を処理する管理レイヤー**です。

#### 身近な例えで理解しよう

ARM は**ホテルのフロントデスク**のようなものです。お客さん（ユーザー）がチェックイン（リソース作成）やルームサービス（リソース変更）をリクエストすると、フロント（ARM）がすべての手配を行います。Portal、CLI、PowerShell、API のどこからリクエストしても、最終的にはすべて ARM を通じて処理されます。

```
  Azure Portal ─┐
  Azure CLI ────┤
  Azure PS ─────┼── → Azure Resource Manager（ARM） → Azure リソース
  REST API ─────┤
  SDK ──────────┘
```

#### ARM の利点
- **一貫性のある管理**: どのツールからでも同じ結果が得られる
- **リソースの依存関係を管理**: 正しい順序でデプロイ
- **アクセス制御（RBAC）**: ARM に統合されている
- **タグ**: リソースの整理に使用
- **テンプレートによるデプロイ**: ARM テンプレートで繰り返しデプロイ可能

> **試験対策ポイント**: 「Azure のすべてのリソース操作が通過する管理レイヤーは？」→ **Azure Resource Manager** です。Portal でも CLI でも、最終的にはすべて ARM を通ります。

---

### ARM テンプレート / Bicep

#### ARM テンプレート

ARM テンプレートは、**Azure リソースをコード（JSON 形式）で定義し、自動的にデプロイするためのファイル**です。

#### 身近な例えで理解しよう

**家具の組み立て説明書**のようなものです。説明書通りに組み立てれば、誰がやっても同じ完成品ができます。ARM テンプレートも同じで、テンプレートに書かれた通りにリソースが作成されるため、毎回同じ環境を確実にデプロイできます。

#### ARM テンプレートの特徴
- **JSON 形式**で記述
- **宣言型**: 「何を作るか」を記述する（「どうやって作るか」ではない）
- **べき等性**: 同じテンプレートを何回実行しても同じ結果になる
- **並列デプロイ**: 依存関係のないリソースは自動的に並列で作成

#### Bicep

Bicep は、**ARM テンプレートをより簡潔に書くための言語**です。

- ARM テンプレート（JSON）は記述が冗長になりがち
- Bicep は**よりシンプルな構文**で同じことができる
- Bicep のコードは自動的に ARM テンプレート（JSON）に変換される

| 項目 | ARM テンプレート | Bicep |
|:---|:---|:---|
| **形式** | JSON | Bicep 独自構文 |
| **可読性** | やや複雑 | シンプルで読みやすい |
| **機能** | 同じ | 同じ |
| **関係** | 基盤 | ARM テンプレートに変換される |

> **試験対策ポイント**: 「宣言型のデプロイ」「JSON 形式のテンプレート」→ **ARM テンプレート** です。「ARM テンプレートよりシンプルな構文」→ **Bicep** です。「べき等性」（何回実行しても同じ結果）もよく問われます。

---

### Infrastructure as Code（IaC）の概念

Infrastructure as Code（IaC）とは、**インフラ環境をコード（ファイル）として定義し、管理する手法**です。

#### 身近な例えで理解しよう

料理を作る時に、毎回「目分量」で作ると味がバラつきますよね。しかし**レシピ（コード）を書いておけば、誰が作っても同じ料理**ができます。IaC はインフラの「レシピ」を書くことで、環境構築のバラつきを無くします。

#### IaC のメリット
- **一貫性**: 毎回同じ環境を確実に構築できる
- **バージョン管理**: コードの変更履歴を追跡できる
- **自動化**: 手作業によるミスを排除
- **再現性**: 開発環境・テスト環境・本番環境を同じ構成で作れる

#### Azure における IaC のツール
- **ARM テンプレート**（JSON）
- **Bicep**
- **Terraform**（サードパーティ）

> **試験対策ポイント**: 「インフラの構成をコードで管理する手法は？」→ **Infrastructure as Code（IaC）** です。ARM テンプレートや Bicep は IaC を実現するための具体的なツールです。

---

## 3.4 Azure の監視ツール

### Azure Advisor

Azure Advisor は、**Azure 環境のベストプラクティスに基づいた推奨事項を提供する無料のサービス**です。

#### 身近な例えで理解しよう

**かかりつけ医の健康アドバイス**のようなものです。「血圧が高めなので塩分を控えましょう」「運動不足です」のように、Azure 環境の改善点を教えてくれます。

#### Azure Advisor の 5 つのカテゴリ

| カテゴリ | 提供するアドバイス |
|:---|:---|
| **信頼性（Reliability）** | アプリケーションの可用性を向上させるための推奨事項 |
| **セキュリティ（Security）** | セキュリティリスクを軽減するための推奨事項 |
| **パフォーマンス（Performance）** | アプリケーションの速度を改善するための推奨事項 |
| **コスト（Cost）** | Azure の支出を最適化するための推奨事項 |
| **オペレーショナルエクセレンス（Operational Excellence）** | 運用の効率化のための推奨事項 |

> **試験対策ポイント**: 「Azure 環境の改善推奨事項を提供するサービスは？」→ **Azure Advisor** です。5 つのカテゴリ（信頼性・セキュリティ・パフォーマンス・コスト・オペレーショナルエクセレンス）も覚えましょう。

---

### Azure Service Health

Azure Service Health は、**Azure サービス自体の稼働状況（障害・メンテナンス情報）を通知するサービス**です。

#### 身近な例えで理解しよう

電力会社の**停電情報サービス**のようなものです。「お住まいの地域で停電が発生しています」「来週メンテナンスのため 2 時間停電します」と教えてくれます。

#### Azure Service Health の 3 つのコンポーネント

| コンポーネント | 範囲 | 説明 |
|:---|:---|:---|
| **Azure Status** | Azure 全体 | Azure の全リージョンの稼働状況をリアルタイムで表示 |
| **Service Health** | 自分が使っているサービス | 利用中のサービスやリージョンに影響する障害・メンテナンスを通知 |
| **Resource Health** | 個々のリソース | 特定のリソース（VM など）の正常性を表示 |

> **試験対策ポイント**: 「Azure のサービス障害やメンテナンス情報を確認したい」→ **Azure Service Health** です。「自分のリソースの正常性を確認したい」→ **Resource Health** です。

---

### Azure Monitor

Azure Monitor は、**Azure リソースのパフォーマンスと正常性を監視するための包括的なプラットフォーム**です。

#### 身近な例えで理解しよう

**車のダッシュボード**のようなものです。速度（CPU 使用率）、燃料残量（ストレージ空き容量）、エンジン温度（メモリ使用率）などをリアルタイムで表示し、異常があれば警告灯（アラート）が点灯します。

#### Azure Monitor の主な機能
- **メトリック（数値データ）** の収集と可視化
- **ログ（テキストデータ）** の収集と分析
- **アラート**の設定
- **ダッシュボード**での可視化

#### Azure Monitor が収集するデータ

| データの種類 | 内容 | 例 |
|:---|:---|:---|
| **メトリック** | 数値データ（時系列） | CPU 使用率、メモリ使用量、ネットワークトラフィック |
| **ログ** | イベントやテキストデータ | エラーログ、アクセスログ、監査ログ |

> **試験対策ポイント**: 「Azure リソースのパフォーマンスを包括的に監視するサービスは？」→ **Azure Monitor** です。メトリック（数値）とログ（テキスト）の 2 種類のデータを扱うことを理解しましょう。

---

### Azure Monitor アラート

Azure Monitor アラートは、**監視条件に一致した場合に自動的に通知やアクションを実行する機能**です。

#### 身近な例えで理解しよう

車の**警告灯と自動通知システム**です。「エンジン温度が 100℃ を超えたら（条件）、ドライバーに警告を出し（通知）、自動でエアコンを最大にする（アクション）」というルールを設定できます。

#### アラートの構成要素

| 要素 | 説明 |
|:---|:---|
| **アラートルール** | 何を監視し、どの条件で発火するかを定義 |
| **アクショングループ** | アラート発火時に何をするかを定義（メール通知、SMS、Webhook など） |
| **アラートの重大度** | 0（重大）～ 4（詳細）の 5 段階 |

> **試験対策ポイント**: 「CPU が 90% を超えたら通知する設定は？」→ **Azure Monitor アラート**です。

---

### Application Insights

Application Insights は、**Web アプリケーションのパフォーマンスと使用状況を監視するサービス**です。Azure Monitor の一部として提供されます。

#### 身近な例えで理解しよう

お店の**来客分析システム**のようなものです。「今日は何人来店したか」「どの商品がよく見られているか」「レジでどのくらい待たされているか」「クレーム（エラー）は何件あったか」を自動で記録・分析してくれます。

#### 主な機能
- **リクエスト率・応答時間**の監視
- **エラー率・例外**の検出
- **ユーザーの行動分析**（ページビュー、セッション数など）
- **依存関係の追跡**（データベースや外部 API の応答時間）
- **スマート検出**（異常パターンの自動検知）

> **試験対策ポイント**: 「Web アプリケーションのパフォーマンス監視」→ **Application Insights** です。Azure Monitor がインフラ全体の監視なのに対し、Application Insights は**アプリケーションレベル**の監視に特化しています。

---

### Azure Log Analytics

Azure Log Analytics は、**Azure Monitor が収集したログデータを検索・分析するためのツール**です。

#### 身近な例えで理解しよう

膨大なレシートの束から「先月の食費の合計を教えて」と聞くと、瞬時に集計して答えてくれる**超高速の経理担当者**のようなものです。

#### 特徴
- **KQL（Kusto Query Language）** という専用のクエリ言語を使用
- Azure Monitor が収集したログを**横断的に検索・分析**
- ダッシュボードやアラートと連携

> **試験対策ポイント**: 「ログデータの検索・分析」→ **Azure Log Analytics** です。使用するクエリ言語は **KQL** であることも覚えておきましょう。

---

### 監視ツールの比較まとめ

| ツール | 主な目的 | 例え |
|:---|:---|:---|
| **Azure Advisor** | ベストプラクティスに基づく**改善提案** | かかりつけ医の健康アドバイス |
| **Azure Service Health** | **Azure 自体の障害・メンテナンス**情報 | 電力会社の停電情報 |
| **Azure Monitor** | リソースの**パフォーマンス監視**（メトリック＋ログ） | 車のダッシュボード |
| **Application Insights** | **Web アプリ**の監視 | お店の来客分析システム |
| **Azure Log Analytics** | **ログの検索・分析** | 超高速の経理担当者 |

> **試験対策ポイント**: この 5 つの監視ツールの**使い分け**は頻出です。特に「Advisor = 提案」「Service Health = Azure の障害情報」「Monitor = リソースの監視」の区別が重要です。

---

## 確認問題

### 問題 1
ある企業が、オンプレミスのデータセンターを Azure に移行した場合のコスト削減効果を経営層に提示したいと考えています。使用すべきツールは次のうちどれですか？

- A) Azure Pricing Calculator
- B) Azure Cost Management and Billing
- C) TCO（総所有コスト）計算ツール
- D) Azure Advisor

---

### 問題 2
ある管理者が、組織内のすべての Azure リソースが特定のリージョン（例：東日本）にのみ作成されるように強制したいと考えています。使用すべきサービスは次のうちどれですか？

- A) Azure RBAC
- B) Azure Policy
- C) リソースロック
- D) Azure Blueprints

---

### 問題 3
ある開発チームが、Azure の仮想マシンの CPU 使用率が 90% を超えた場合にメール通知を受け取りたいと考えています。使用すべきサービスは次のうちどれですか？

- A) Azure Advisor
- B) Azure Service Health
- C) Azure Monitor アラート
- D) Azure Log Analytics

---

## 解答・解説

### 問題 1 の解答: **C) TCO（総所有コスト）計算ツール**

**解説**: TCO 計算ツールは、**オンプレミス環境と Azure のコストを比較**するためのツールです。サーバー、ストレージ、ネットワーク、人件費、電力費などを含めた総コストで比較し、Azure に移行することでどれだけ削減できるかを示すレポートを生成します。Pricing Calculator は Azure サービスの料金見積もりに使うツールで、オンプレミスとの比較はできません。Cost Management は既に Azure を使用中のコスト管理ツールです。

---

### 問題 2 の解答: **B) Azure Policy**

**解説**: Azure Policy は、**リソースに対するルールを強制するサービス**です。「許可されたリージョン」ポリシーを設定することで、指定されたリージョン以外でのリソース作成を拒否できます。RBAC は「誰が何をできるか」を管理するもので、リソースの構成ルールを強制するものではありません。リソースロックは既存リソースの誤削除・誤変更の防止です。

---

### 問題 3 の解答: **C) Azure Monitor アラート**

**解説**: Azure Monitor アラートは、**メトリック（CPU 使用率など）が指定した条件を超えた場合に、通知やアクションを実行する機能**です。CPU 使用率の監視条件を設定し、アクショングループでメール通知を構成することで、しきい値超過時に自動通知を受け取れます。Azure Advisor は改善提案、Service Health は Azure 自体の障害情報、Log Analytics はログの分析ツールであり、リアルタイムの条件ベース通知には Azure Monitor アラートが最適です。
