# 2. Azure のアーキテクチャとサービス

> この章では、Azure の物理的・論理的な構造と、主要なサービスについて学びます。
> AZ-900 試験全体の **35～40%** を占める、最もボリュームの大きい分野です。

---

## 2.1 Azure の主要なアーキテクチャコンポーネント

### Azure リージョン、リージョンペア、ソブリンリージョン

#### Azure リージョンとは

Azure リージョンとは、**1 つ以上のデータセンターが集まった地理的なエリア**のことです。世界中に 60 以上のリージョンがあります。

#### 身近な例えで理解しよう

リージョンは**コンビニのチェーン店舗**のようなものです。東京店、大阪店、ニューヨーク店…と世界中に店舗があり、お客さんは一番近い店舗を利用します。Azure も同じで、ユーザーに近いリージョンを選ぶことで、通信の遅延（レイテンシ）を最小限にできます。

#### リージョンペアとは

Azure リージョンは、**同じ地域内の別のリージョンとペア**になっています。これを**リージョンペア**と呼びます。

- 例：**東日本リージョン** ⇔ **西日本リージョン**
- 片方のリージョンで大規模障害が発生しても、ペアのリージョンがバックアップとして機能する
- Azure のアップデートは、ペアのリージョンに同時には適用されない（同時にダウンするリスクを回避）

#### ソブリンリージョンとは

**特定の国や政府の規制に準拠するために、通常の Azure ネットワークから物理的に分離されたリージョン**です。

| ソブリンリージョン | 対象 |
|:---|:---|
| **Azure Government** | 米国政府機関向け |
| **Azure China（21Vianet）** | 中国国内向け（21Vianet が運営） |

> **試験対策ポイント**: 「リージョンペアの目的は？」→ **災害復旧（ディザスタリカバリ）** です。「ソブリンリージョンの特徴は？」→ **通常の Azure から物理的に分離されている**ことを覚えましょう。

---

### 可用性ゾーン（Availability Zones）

可用性ゾーンとは、**1 つのリージョン内にある、物理的に独立したデータセンターのグループ**です。各ゾーンは独自の電源、冷却装置、ネットワークを持っています。

#### 身近な例えで理解しよう

**同じ都市内に 3 つの病院がある**状態を想像してください。1 つの病院が停電しても、残り 2 つの病院が患者を受け入れられます。可用性ゾーンも同じで、1 つのデータセンターに障害が起きても、他のゾーンでサービスが継続されます。

#### 可用性ゾーンの構成

```
  リージョン（例：東日本）
  ┌──────────────────────────────────┐
  │  ゾーン 1    ゾーン 2    ゾーン 3  │
  │  [DC群]     [DC群]     [DC群]    │
  │  独自電源    独自電源    独自電源   │
  │  独自冷却    独自冷却    独自冷却   │
  └──────────────────────────────────┘
```

- 1 つのリージョンには**最低 3 つの可用性ゾーン**がある
- 各ゾーンは物理的に離れた場所にある
- 高速ネットワークで相互接続されている

> **試験対策ポイント**: 「可用性ゾーンは何のために使う？」→ **単一データセンターの障害からアプリケーションを保護するため**です。「リージョンペア」はリージョン全体の障害対策、「可用性ゾーン」はリージョン内のデータセンター障害対策です。

---

### Azure データセンター

Azure データセンターは、**Azure のサービスが実際に稼働している物理的な建物**です。

- 世界中に分散配置されている
- 厳重なセキュリティ（生体認証、監視カメラ、セキュリティゲートなど）で保護
- 冗長化された電源と冷却システムを備えている
- ユーザーが直接データセンターにアクセスすることはできない

> **試験対策ポイント**: データセンターの物理的なセキュリティは**常にクラウドプロバイダー（Microsoft）の責任**です。

---

### Azure リソースとリソースグループ

#### Azure リソースとは

Azure リソースとは、**Azure で作成・利用するすべてのもの**です。仮想マシン、データベース、ストレージアカウント、仮想ネットワークなど、すべてが「リソース」です。

#### リソースグループとは

リソースグループとは、**関連するリソースをまとめて管理するための入れ物（コンテナ）** です。

#### 身近な例えで理解しよう

**書類をフォルダに分けて整理する**のと同じです。「プロジェクト A」のフォルダには、プロジェクト A に関係する書類をすべて入れます。

#### リソースグループのルール

- すべてのリソースは、**必ず 1 つのリソースグループに所属**しなければならない
- 1 つのリソースが**複数のリソースグループに同時に所属することはできない**
- リソースグループ自体はリージョンを持つが、中のリソースは別のリージョンに配置できる
- リソースグループを**削除すると、中のリソースもすべて削除**される
- リソースは別のリソースグループに**移動**できる

> **試験対策ポイント**: 「1 つのリソースは複数のリソースグループに所属できるか？」→ **できない**（1 対 1）。「リソースグループを削除するとどうなるか？」→ **中のリソースもすべて削除される**。頻出です。

---

### サブスクリプション

サブスクリプションとは、**Azure サービスを利用するための契約単位**であり、**課金の境界**でもあります。

#### 身近な例えで理解しよう

**携帯電話の回線契約**を考えてください。1 つの契約（サブスクリプション）に対して月額料金が請求されます。家族でそれぞれ別の回線契約を持つこともできますよね。Azure も同じで、部署ごとやプロジェクトごとに別々のサブスクリプションを作って、コストを分けて管理できます。

#### サブスクリプションの 2 つの境界

| 境界 | 意味 |
|:---|:---|
| **課金の境界** | サブスクリプションごとに別々の請求書が発行される |
| **アクセス制御の境界** | サブスクリプションごとにアクセス権限を設定できる |

> **試験対策ポイント**: 「部門ごとに別々の請求書を受け取りたい場合はどうする？」→ **部門ごとに別のサブスクリプションを作成する**が正解です。

---

### 管理グループ（Management Groups）

管理グループとは、**複数のサブスクリプションをまとめて管理するための上位の入れ物**です。

#### 身近な例えで理解しよう

大きな会社の**組織図**を想像してください。「本社」の下に「営業部」「開発部」「経理部」があり、各部署の下にチームがある。管理グループはこの組織図のようなもので、上位のグループに設定したルール（ポリシー）は、下位のすべてのサブスクリプションに自動的に適用（継承）されます。

#### 管理グループの特徴

- 最大 **6 階層**までネストできる（ルート管理グループを除く）
- 1 つのサブスクリプションは **1 つの管理グループ**にのみ所属
- 管理グループに適用したポリシーは下位に**継承**される

> **試験対策ポイント**: 管理グループは「複数のサブスクリプションにまたがるポリシーを一括で適用したい」場合に使います。

---

### リソースグループ・サブスクリプション・管理グループの階層構造

Azure のリソース管理は、以下の**階層構造**になっています。

```
  ルート管理グループ
    └── 管理グループ
          └── サブスクリプション
                └── リソースグループ
                      └── リソース（VM、DB、ストレージなど）
```

| レベル | 役割 | 例え |
|:---|:---|:---|
| **管理グループ** | サブスクリプションをまとめてポリシーを適用 | 会社全体の規則 |
| **サブスクリプション** | 課金とアクセスの境界 | 部門の予算枠 |
| **リソースグループ** | リソースの論理的なまとまり | プロジェクトフォルダ |
| **リソース** | 実際に利用するサービス | 個々の書類 |

**重要**: 上位レベルで設定したポリシーやアクセス権限は、下位レベルに**継承**されます。

> **試験対策ポイント**: この階層構造の「上から下への順番」と「継承」の概念は非常によく出題されます。特に「管理グループ → サブスクリプション → リソースグループ → リソース」の順番を確実に覚えましょう。

---

## 2.2 Azure のコンピューティングサービスとネットワークサービス

### コンピューティングサービス

#### Azure Virtual Machines（VM）

Azure Virtual Machines は、**クラウド上に作成できる仮想的なコンピューター**です。

#### 身近な例えで理解しよう

VM は**レンタカー**のようなものです。車（コンピューター）を買わなくても、必要な時にレンタルして使えます。車種（OS）やオプション（CPU、メモリ）も自由に選べますが、運転（OS の管理やアプリの設定）は自分で行います。

#### VM の特徴
- **IaaS** に分類される
- OS を自由に選択できる（Windows / Linux）
- CPU、メモリ、ディスクのサイズを選べる
- OS のパッチ適用やセキュリティ設定は**ユーザーの責任**
- 既存のオンプレミス環境をそのままクラウドに移行（**リフト＆シフト**）する場合に最適

> **試験対策ポイント**: VM は **IaaS** の代表例です。「リフト＆シフト（既存アプリをそのままクラウドに移行）に最適なサービスは？」→ **Azure VM** です。

---

#### Azure Virtual Machine Scale Sets

Virtual Machine Scale Sets は、**同じ構成の VM を自動的に増減させる仕組み**です。

#### 身近な例えで理解しよう

お祭りの**屋台**を想像してください。お客さんが少ない平日は 1 台、混雑するお祭り当日は 10 台に自動で増やし、終わったら 1 台に戻す。Scale Sets はこれと同じことを VM で行います。

#### Scale Sets の特徴
- 需要に応じて VM の数を**自動スケール**
- すべての VM が同じ構成（同じ OS、同じアプリ）
- 負荷分散（ロードバランサー）と組み合わせて使用
- 高可用性と自動スケーリングを実現

> **試験対策ポイント**: 「需要に応じて VM の数を自動で増減させたい」→ **Virtual Machine Scale Sets** です。

---

#### Azure Virtual Desktop

Azure Virtual Desktop は、**クラウド上で動作する仮想のデスクトップ環境**です。

#### 身近な例えで理解しよう

会社のパソコンを自宅から遠隔操作する**リモートデスクトップ**のクラウド版です。自分のパソコンやタブレットからインターネット経由でアクセスすると、会社と同じデスクトップ環境が使えます。

#### 特徴
- どこからでもアクセスできる
- Windows 10 / 11 のマルチセッション対応（1 つの VM を複数ユーザーで共有）
- セキュリティ対策としてデータを端末に残さない

> **試験対策ポイント**: 「リモートワークで安全にデスクトップ環境を提供したい」→ **Azure Virtual Desktop** です。

---

#### Azure Containers（Azure Container Instances）

コンテナとは、**アプリケーションとその実行に必要なものを 1 つのパッケージにまとめた軽量な仮想化技術**です。

#### 身近な例えで理解しよう

VM が**一軒家の引っ越し**だとすると、コンテナは**引っ越し用の段ボール箱**です。段ボール箱にすべて詰めてあるので、どの家（サーバー）に持って行ってもすぐに開梱して使えます。一軒家（VM）と違って、OS を丸ごと含まないので軽量で素早く起動します。

#### Azure Container Instances（ACI）の特徴
- VM より**軽量で高速に起動**
- OS を含まない（ホスト OS を共有）
- 1 つのマシン上で**複数のコンテナ**を実行可能
- 簡単なコンテナ実行に最適（管理ツール不要）

> **試験対策ポイント**: 「VM とコンテナの違い」は頻出です。コンテナは **OS を含まず軽量**、VM は **OS を含みフル機能**と覚えましょう。

---

#### Azure Kubernetes Service（AKS）

AKS は、**大量のコンテナを効率的に管理・運用するためのサービス**です。

#### 身近な例えで理解しよう

コンテナが段ボール箱だとすると、AKS は**大規模な倉庫の管理システム**です。何千もの段ボール箱がどこにあるか、壊れた箱がないか、箱が足りなくなっていないかを自動で管理します。

#### AKS の特徴
- コンテナの**オーケストレーション**（自動配置、スケーリング、管理）
- コンテナの正常性を監視し、異常があれば自動復旧
- 大規模なコンテナ環境に最適

> **試験対策ポイント**: 「コンテナのオーケストレーション」というキーワードが出たら **AKS** です。少数のコンテナなら **ACI**、大規模な管理なら **AKS** と使い分けます。

---

#### Azure Functions（サーバーレス）

Azure Functions は、**イベントに応じてコードを実行するサーバーレスのコンピューティングサービス**です。

#### 身近な例えで理解しよう

**自動販売機**を想像してください。お金を入れてボタンを押す（イベント発生）と、飲み物が出てくる（コードが実行される）。誰もボタンを押さない時間帯は電気代がほとんどかかりません。サーバーの管理も自動販売機のメーカーがやってくれます。

#### Azure Functions の特徴
- **サーバーレス**: サーバーの管理が一切不要
- **イベント駆動**: 何かが起きた時だけコードが実行される
- **従量課金**: コードが実行された回数と時間に対してのみ課金
- コードが実行されていない間は**料金がかからない**

> **試験対策ポイント**: 「サーバーの管理なしでコードを実行したい」「イベント駆動型」→ **Azure Functions** です。「コードが実行されていない時は課金されない」も重要なポイントです。

---

#### Azure App Service

Azure App Service は、**Web アプリケーション、REST API、モバイルバックエンドをホスティングするための PaaS サービス**です。

#### 身近な例えで理解しよう

**レンタルオフィス**のようなものです。机、椅子、インターネット回線、電話はすべて用意されています。あなたはノートパソコン（アプリケーションのコード）を持ち込むだけで仕事を始められます。

#### App Service の特徴
- **PaaS** に分類される
- OS やインフラの管理は Azure が担当
- 自動スケーリング、CI/CD（継続的デプロイ）に対応
- .NET、Java、Node.js、Python など多くの言語をサポート

> **試験対策ポイント**: App Service は **PaaS** です。「Web アプリをホスティングしたいが、OS の管理はしたくない」→ **Azure App Service** が正解です。

---

#### コンピューティングの種類の比較（VM / コンテナ / サーバーレス）

| 項目 | VM | コンテナ（ACI / AKS） | サーバーレス（Functions） |
|:---|:---|:---|:---|
| **例え** | レンタカー | 段ボール引っ越し | 自動販売機 |
| **分類** | IaaS | IaaS～PaaS | PaaS |
| **OS の管理** | ユーザー | 部分的にユーザー | 不要 |
| **起動速度** | 遅い（分単位） | 速い（秒単位） | 最速（ミリ秒単位） |
| **柔軟性** | 最も高い | 高い | コード実行に限定 |
| **課金** | 起動中は常に課金 | 起動中は常に課金 | 実行時のみ課金 |
| **適したケース** | リフト＆シフト | マイクロサービス | イベント駆動処理 |

> **試験対策ポイント**: シナリオ問題で「どのサービスを使うべきか？」を判断する力が求められます。管理の手間を減らしたい → コンテナやサーバーレス、完全なコントロールが必要 → VM と考えましょう。

---

### ネットワークサービス

#### Azure Virtual Network（VNet）とサブネット

Azure Virtual Network（VNet）は、**Azure 上に作成する仮想的なプライベートネットワーク**です。

#### 身近な例えで理解しよう

VNet は**オフィスビルの社内ネットワーク**のようなものです。ビル内の各部屋（VM やサービス）は社内ネットワークで自由に通信できますが、外部からは簡単にアクセスできません。

**サブネット**は、そのビル内の**フロア分け**です。1 階は受付（Web サーバー）、2 階はオフィス（アプリサーバー）、3 階は金庫室（データベース）のように、ネットワークを区分けして管理します。

#### VNet の特徴
- Azure リソース同士の通信を可能にする
- サブネットに分割してトラフィックを制御
- オンプレミスネットワークとの接続も可能
- VNet は**特定のリージョン**に作成される

> **試験対策ポイント**: 「Azure リソース間の通信を可能にするサービスは？」→ **Azure Virtual Network** です。VNet はリージョンに紐づくことも覚えましょう。

---

#### Azure VNet ピアリング

VNet ピアリングは、**2 つの VNet を接続して、相互に通信できるようにする機能**です。

#### 身近な例えで理解しよう

隣り合う 2 つのオフィスビルの間に**連絡通路（渡り廊下）** を作るようなものです。それぞれのビルは独立していますが、連絡通路を通じて行き来できるようになります。

#### VNet ピアリングの種類

| 種類 | 接続先 |
|:---|:---|
| **リージョン VNet ピアリング** | 同じリージョン内の VNet 同士 |
| **グローバル VNet ピアリング** | 異なるリージョンの VNet 同士 |

- トラフィックは Microsoft のバックボーンネットワークを通る（インターネットを経由しない）
- 低遅延、高帯域幅

> **試験対策ポイント**: 「異なるリージョンの VNet を接続したい」→ **グローバル VNet ピアリング**です。通信は Microsoft のプライベートネットワークを経由します。

---

#### Azure VPN Gateway

VPN Gateway は、**暗号化されたトンネルを通じて、オンプレミスネットワークと Azure VNet を安全に接続するサービス**です。

#### 身近な例えで理解しよう

自宅（オンプレミス）と会社（Azure）の間に**秘密の地下トンネル**を作るようなものです。トンネルの中を通る情報は暗号化されているので、外部から盗み見られる心配がありません。

#### VPN の種類

| 種類 | 特徴 |
|:---|:---|
| **サイト間（S2S）VPN** | オンプレミスのネットワーク全体を Azure に接続 |
| **ポイント対サイト（P2S）VPN** | 個々のデバイス（PC など）から Azure に接続 |

- インターネットを経由するが、**暗号化されたトンネル**で保護
- ExpressRoute より安価だが、速度と信頼性は劣る

> **試験対策ポイント**: 「インターネット経由で暗号化された接続」→ **VPN Gateway**。「個人のリモートアクセス」→ **ポイント対サイト VPN**。「拠点間の接続」→ **サイト間 VPN** です。

---

#### Azure ExpressRoute

ExpressRoute は、**インターネットを経由せず、専用の回線でオンプレミスと Azure を直接接続するサービス**です。

#### 身近な例えで理解しよう

VPN が「一般道路に作った秘密のトンネル」だとすれば、ExpressRoute は**専用の高速道路**です。他の車（トラフィック）と混ざらないので、速度が安定しており信頼性が非常に高いです。

#### ExpressRoute の特徴
- **インターネットを経由しない**（専用回線）
- 高帯域幅、低遅延、高信頼性
- VPN Gateway より**高コスト**
- 大量データの転送や、高い信頼性が求められる企業向け

| 項目 | VPN Gateway | ExpressRoute |
|:---|:---|:---|
| **接続経路** | インターネット経由 | 専用回線（プライベート接続） |
| **暗号化** | あり | オプション |
| **帯域幅** | 中程度 | 非常に高い |
| **コスト** | 安い | 高い |
| **信頼性** | 中程度 | 非常に高い |
| **適したケース** | 小～中規模の接続 | 大規模・ミッションクリティカル |

> **試験対策ポイント**: 「インターネットを経由しないプライベート接続」→ **ExpressRoute** です。VPN Gateway との違いは最頻出です。

---

#### Azure DNS

Azure DNS は、**DNS ドメインのホスティングサービス**です。ドメイン名（例：www.example.com）を IP アドレスに変換する役割を担います。

#### 身近な例えで理解しよう

DNS は**電話帳**のようなものです。友人の名前（ドメイン名）を検索すると、電話番号（IP アドレス）が分かります。Azure DNS はこの電話帳をクラウド上で管理するサービスです。

#### 特徴
- Azure の他のサービスと統合して管理できる
- RBAC やリソースロックで保護できる
- **ドメイン名の購入はできない**（別途レジストラで購入が必要）

> **試験対策ポイント**: Azure DNS は「DNS レコードの管理」はできますが、「ドメイン名の購入」はできません。

---

#### パブリックエンドポイントとプライベートエンドポイント

| 種類 | 意味 | 例え |
|:---|:---|:---|
| **パブリックエンドポイント** | インターネットからアクセス可能なアドレス | お店の**表玄関**（誰でも入れる） |
| **プライベートエンドポイント** | VNet 内からのみアクセス可能なアドレス | お店の**従業員専用通用口** |

- パブリックエンドポイントはインターネットに公開されるため、セキュリティ対策が重要
- プライベートエンドポイントを使うと、Azure サービスを VNet 内のプライベート IP アドレスで利用できる

> **試験対策ポイント**: 「Azure サービスへのアクセスをプライベートネットワークに限定したい」→ **プライベートエンドポイント**です。

---

#### Azure ネットワークセキュリティグループ（NSG）

NSG は、**Azure リソースへのネットワークトラフィック（通信）をフィルタリングするためのファイアウォールのような機能**です。

#### 身近な例えで理解しよう

建物の**警備員**のようなものです。「社員証を持っている人は入れるけど、部外者は入れない」「宅配便は裏口からのみ受け付ける」といったルールを設定できます。

#### NSG の特徴
- **受信規則**と**送信規則**を設定
- 各規則は**優先度**を持ち、番号が小さいほど優先される
- サブネットまたは NIC（ネットワークインターフェース）に関連付ける
- 許可（Allow）と拒否（Deny）のルールを設定できる

> **試験対策ポイント**: 「VNet 内のトラフィックを制御するには？」→ **NSG** です。NSG は「サブネット」と「NIC」の両方に適用できることを覚えましょう。

---

## 2.3 Azure のストレージサービス

### Azure Storage アカウント

Azure Storage アカウントは、**Azure のストレージサービスを利用するための入れ物**です。Blob、Files、Queue、Table など、すべてのストレージサービスはストレージアカウントの中に作成されます。

#### 身近な例えで理解しよう

ストレージアカウントは**銀行の口座**のようなものです。口座を開設すると、普通預金（Blob）、定期預金（アーカイブ）、小切手（Queue）など、さまざまなサービスが使えるようになります。

#### 特徴
- ストレージアカウント名は **Azure 全体で一意**でなければならない
- **URL でアクセス可能**（例：`https://<アカウント名>.blob.core.windows.net`）
- 冗長性オプションを選択できる

> **試験対策ポイント**: 「ストレージアカウント名は一意である」という点が問われることがあります。

---

### Azure Storage の冗長性オプション（LRS / ZRS / GRS / RA-GRS / GZRS / RA-GZRS）

Azure Storage はデータの損失を防ぐために、常に**複数のコピー**を保持しています。

| 冗長性オプション | コピー数 | 範囲 | 説明 |
|:---|:---:|:---|:---|
| **LRS**（ローカル冗長ストレージ） | 3 | 単一データセンター内 | 同じデータセンター内に 3 つのコピー |
| **ZRS**（ゾーン冗長ストレージ） | 3 | 同一リージョン内の 3 つの可用性ゾーン | 各ゾーンに 1 つずつコピー |
| **GRS**（geo 冗長ストレージ） | 6 | プライマリ（LRS）+ セカンダリリージョン（LRS） | 2 つのリージョンに合計 6 コピー |
| **RA-GRS**（読み取りアクセス geo 冗長） | 6 | GRS と同じ + セカンダリから読み取り可能 | GRS に加え、セカンダリからの読み取りが可能 |
| **GZRS**（geo ゾーン冗長ストレージ） | 6 | プライマリ（ZRS）+ セカンダリリージョン（LRS） | 最高レベルの可用性 |
| **RA-GZRS**（読み取りアクセス geo ゾーン冗長） | 6 | GZRS と同じ + セカンダリから読み取り可能 | 最高レベルの可用性 + 読み取りアクセス |

#### 身近な例えで理解しよう

- **LRS**: 大事な書類を**同じ金庫の中に 3 部コピー**して保管。金庫が壊れたら全滅
- **ZRS**: **3 つの別々の金庫**にそれぞれ 1 部ずつ保管。1 つの金庫が壊れても大丈夫
- **GRS**: 東京と大阪の**2 つのオフィスに 3 部ずつ**保管。東京が被災しても大阪にある
- **RA-GRS**: GRS と同じだが、大阪のオフィスからも**読み出せる**

> **試験対策ポイント**: 冗長性オプションの比較は頻出です。特に「LRS と ZRS の違い」（データセンター障害に耐えられるのは ZRS）と「GRS と RA-GRS の違い」（セカンダリからの読み取りの有無）を理解しましょう。**最も低コスト = LRS**、**最も高い可用性 = RA-GZRS** です。

---

### Azure Blob Storage

Blob Storage は、**非構造化データ（テキスト、画像、動画、バックアップなど）を保存するためのストレージ**です。

#### 身近な例えで理解しよう

**トランクルーム**のようなものです。箱の中には何でも入れられます。書類でも家具でも、サイズや種類を問わず保管できます。

#### Blob の種類

| 種類 | 用途 |
|:---|:---|
| **ブロック Blob** | テキストやバイナリデータの保存（最も一般的） |
| **追加 Blob** | ログファイルなど、追記専用のデータ |
| **ページ Blob** | 仮想マシンのディスク（VHD ファイル） |

> **試験対策ポイント**: Blob Storage は「非構造化データ」の保存に使います。「画像や動画の保存に適したサービスは？」→ **Azure Blob Storage** です。

---

### Azure Blob のアクセス層（ホット / クール / コールド / アーカイブ）

Blob のアクセス層は、**データのアクセス頻度に応じてコストを最適化する仕組み**です。

| アクセス層 | アクセス頻度 | ストレージコスト | アクセスコスト | 例え |
|:---|:---|:---|:---|:---|
| **ホット** | 頻繁にアクセス | 高い | 低い | **冷蔵庫**（すぐ取り出せる） |
| **クール** | 30 日以上保存、たまにアクセス | やや低い | やや高い | **パントリー**（たまに使う食材） |
| **コールド** | 90 日以上保存、めったにアクセスしない | 低い | 高い | **地下倉庫**（季節もの） |
| **アーカイブ** | 180 日以上保存、ほぼアクセスしない | 最も低い | 最も高い | **貸し倉庫**（取り出すのに時間がかかる） |

**重要**: アーカイブ層のデータにアクセスするには、まず別の層に**リハイドレート（戻す）** する必要があり、数時間かかります。

> **試験対策ポイント**: 「ストレージコストが最も安い層は？」→ **アーカイブ**。「アクセスコストが最も安い層は？」→ **ホット**。このトレードオフの理解が重要です。アーカイブ層からのデータ取得に時間がかかる点も頻出です。

---

### Azure Files

Azure Files は、**クラウド上に作成できるフルマネージドのファイル共有サービス**です。

#### 身近な例えで理解しよう

オフィスの**共有フォルダ（ファイルサーバー）** のクラウド版です。複数のパソコンから同じフォルダにアクセスして、ファイルを読み書きできます。

#### 特徴
- **SMB（Server Message Block）** プロトコルと **NFS** プロトコルに対応
- Windows、Linux、macOS からアクセス可能
- オンプレミスのファイルサーバーの置き換えに最適

> **試験対策ポイント**: 「オンプレミスのファイルサーバーをクラウドに移行したい」→ **Azure Files** です。SMB プロトコルに対応している点も覚えましょう。

---

### Azure Queue Storage

Queue Storage は、**大量のメッセージを保存し、順番に処理するためのサービス**です。

#### 身近な例えで理解しよう

レストランの**注文伝票を順番に並べるフック**のようなものです。ウェイターが注文を書いて掛けていき、シェフが順番に取って料理します。注文が殺到しても、伝票は溜まるだけで失われません。

> **試験対策ポイント**: 「非同期メッセージング」「メッセージの順次処理」→ **Azure Queue Storage** です。

---

### Azure Table Storage

Table Storage は、**大量の構造化データ（キーと値のペア）を保存するための NoSQL データストア**です。

#### 身近な例えで理解しよう

**巨大な Excel の表**を想像してください。行ごとにデータを保存し、キー（行番号のようなもの）を指定して素早く検索できます。ただし、リレーショナルデータベースのような複雑な結合はできません。

> **試験対策ポイント**: Table Storage は「大量のキーバリュー型データを低コストで保存する」サービスです。リレーショナルではなく **NoSQL** であることがポイントです。

---

### Azure Disk Storage

Azure Disk Storage は、**Azure VM に接続するための仮想ディスク**です。

#### 身近な例えで理解しよう

パソコンに**外付けハードディスクを接続する**のと同じです。VM に追加のストレージが必要な場合に、ディスクを追加して使います。

#### ディスクの種類

| 種類 | 特徴 | 適したケース |
|:---|:---|:---|
| **Ultra Disk** | 最高性能 | データベースなどの高 I/O ワークロード |
| **Premium SSD** | 高性能 | 本番環境 |
| **Standard SSD** | 中程度の性能 | Web サーバー、軽量アプリ |
| **Standard HDD** | 低コスト | バックアップ、アクセス頻度の低いデータ |

> **試験対策ポイント**: 「VM に接続するストレージ」→ **Azure Disk Storage** です。

---

### Azure のデータ移行オプション（AzCopy / Azure Storage Explorer / Azure File Sync）

| ツール | 概要 | 特徴 |
|:---|:---|:---|
| **AzCopy** | コマンドラインのデータ転送ツール | 大量データの高速コピーに最適、スクリプト化が容易 |
| **Azure Storage Explorer** | GUI ベースのデータ管理ツール | 視覚的にファイルを操作、初心者でも使いやすい |
| **Azure File Sync** | オンプレミスのファイルサーバーと Azure Files を同期 | ファイルサーバーのクラウド拡張 |

#### 身近な例えで理解しよう

- **AzCopy**: **引っ越し業者のトラック**（大量の荷物を効率よく運ぶ）
- **Azure Storage Explorer**: **ファイルマネージャー**（ドラッグ＆ドロップで操作）
- **Azure File Sync**: **クラウド同期アプリ**（Dropbox のように自動同期）

> **試験対策ポイント**: 「大量データをコマンドラインで転送」→ **AzCopy**。「GUI で管理」→ **Azure Storage Explorer**。「オンプレミスとクラウドの同期」→ **Azure File Sync** です。

---

### Azure Migrate

Azure Migrate は、**オンプレミスの環境を Azure に移行するための統合ツール**です。

#### 特徴
- オンプレミスのサーバー、データベース、Web アプリの**検出と評価**
- 移行の**計画と実行**を支援
- 移行のコストと準備状況を**評価**

#### 身近な例えで理解しよう

新居に引っ越す前の**引っ越しコンサルタント**です。「何を持っていくか」「新居にどう配置するか」「費用はいくらか」をすべて調べてくれます。

> **試験対策ポイント**: 「オンプレミスからAzureへの移行を評価・計画するサービスは？」→ **Azure Migrate** です。

---

### Azure Data Box

Azure Data Box は、**物理的なストレージデバイスを使って、大量のデータを Azure に転送するサービス**です。

#### 身近な例えで理解しよう

インターネット回線で何日もかかるような大量データを、**宅配便でハードディスクを送って転送する**方法です。USBメモリを郵送するようなものですが、もっと大規模です。

#### 特徴
- ネットワーク帯域が限られている場合や、数十 TB 以上のデータ移行に最適
- Microsoft がデバイスを送付 → データをコピー → 返送 → Azure にアップロード
- データは暗号化されて安全に転送

> **試験対策ポイント**: 「ネットワークが遅い/大量データの移行」→ **Azure Data Box** です。物理デバイスを使ったオフライン転送という点が特徴です。

---

## 2.4 Azure の ID、アクセス、セキュリティ

### Microsoft Entra ID（旧 Azure Active Directory）

Microsoft Entra ID は、**クラウドベースの ID およびアクセス管理サービス**です。「誰がアクセスできるか」を管理する Azure のセキュリティの要です。

#### 身近な例えで理解しよう

会社の**受付と入館証システム**のようなものです。受付で本人確認（認証）を行い、入館証（トークン）を発行します。入館証には「この人は 3 階まで入れる」などの権限情報（認可）が含まれています。

#### Microsoft Entra ID の主な機能
- **認証**: ユーザーの本人確認（サインイン）
- **シングルサインオン（SSO）**: 一度のログインで複数のアプリにアクセス
- **アプリケーション管理**: SaaS アプリとの連携
- **デバイス管理**: デバイスの登録と管理

#### オンプレミスの Active Directory との違い

| 項目 | オンプレミス AD | Microsoft Entra ID |
|:---|:---|:---|
| **プロトコル** | Kerberos / LDAP | OAuth / OpenID Connect / SAML |
| **場所** | 社内ネットワーク | クラウド |
| **管理対象** | 社内デバイス・ユーザー | クラウドアプリ・外部ユーザーも含む |

> **試験対策ポイント**: 「クラウドベースの ID 管理サービスは？」→ **Microsoft Entra ID** です。旧名称の「Azure Active Directory（Azure AD）」で出題される場合もあります。オンプレミスの AD DS とは**別のサービス**であることも重要です。

---

### Microsoft Entra ID の認証方法（SSO / MFA / パスワードレス）

#### シングルサインオン（SSO）

**一度のログインで複数のアプリケーションにアクセスできる仕組み**です。

例え: 遊園地の**フリーパス**。1 回チケットを買えば、園内のどの乗り物にも乗れます。

#### 多要素認証（MFA）

**2 つ以上の要素を使って本人確認を行う仕組み**です。

| 要素の種類 | 内容 | 例 |
|:---|:---|:---|
| **知識情報** | 自分だけが知っていること | パスワード、PIN |
| **所持情報** | 自分だけが持っているもの | スマートフォン、セキュリティキー |
| **生体情報** | 自分自身の身体的特徴 | 指紋、顔認証 |

例え: 銀行の ATM で**キャッシュカード（所持情報）＋暗証番号（知識情報）** の 2 つを使うのと同じです。

#### パスワードレス認証

**パスワードを使わずに認証を行う方法**です。

- **Windows Hello**: 顔認証や指紋認証
- **Microsoft Authenticator アプリ**: スマートフォンでの承認
- **FIDO2 セキュリティキー**: 物理的なセキュリティキー

> **試験対策ポイント**: MFA の「3 つの要素」は頻出です。「パスワード + スマートフォンの確認コード」は MFA の典型例。「知識 + 所持 + 生体」のどれに該当するか判断できるようにしましょう。

---

### Microsoft Entra External ID（B2B / B2C）

| 種類 | 対象 | 例え |
|:---|:---|:---|
| **B2B（Business to Business）** | 外部の企業・パートナー | **来客用の入館証**を発行して、社内の一部にアクセスを許可 |
| **B2C（Business to Consumer）** | 一般消費者・顧客 | **お店の会員カード**で、お客さんがログインしてサービスを利用 |

- **B2B**: パートナー企業の社員を自社のアプリに招待
- **B2C**: 一般ユーザーが Google や Facebook のアカウントでサインアップ

> **試験対策ポイント**: 「外部のパートナー企業とコラボレーション」→ **B2B**。「消費者向けアプリのサインイン管理」→ **B2C** です。

---

### Microsoft Entra 条件付きアクセス

条件付きアクセスは、**特定の条件に基づいてアクセスを許可・拒否・追加認証を要求するポリシー**です。

#### 身近な例えで理解しよう

空港の**セキュリティチェック**のようなものです。「国際線の乗客（条件）はパスポートの提示（追加認証）が必要」「国内線の乗客（別の条件）は搭乗券だけでOK」というルールを設定できます。

#### 条件（シグナル）の例
- **ユーザーまたはグループ**: 特定の人だけに適用
- **場所**: 社内ネットワークからは許可、社外からは MFA を要求
- **デバイス**: 会社管理のデバイスからのみ許可
- **アプリケーション**: 特定のアプリへのアクセス時のみ追加認証
- **リスクレベル**: 不審なサインイン検出時にブロック

> **試験対策ポイント**: 「社外からのアクセス時にのみ MFA を要求したい」→ **条件付きアクセス**です。条件（if）とアクション（then）の組み合わせで制御することを理解しましょう。

---

### Azure のロールベースアクセス制御（RBAC）

RBAC は、**「誰が」「どのリソースに」「何をできるか」をロール（役割）で管理する仕組み**です。

#### 身近な例えで理解しよう

会社の**役職に応じたアクセス権**のようなものです。「社長はすべてのフロアに入れる」「一般社員は自分のフロアのみ」「清掃員は共用部分のみ」というルールです。

#### RBAC の主要な組み込みロール

| ロール | 権限 |
|:---|:---|
| **所有者（Owner）** | すべてのリソースへのフルアクセス + 他者への権限付与 |
| **共同作成者（Contributor）** | すべてのリソースの作成・管理（ただし権限の付与はできない） |
| **閲覧者（Reader）** | リソースの読み取りのみ |

#### RBAC の適用範囲

RBAC は以下の**スコープ（範囲）** に適用でき、上位で設定した権限は下位に**継承**されます。

```
管理グループ → サブスクリプション → リソースグループ → リソース
```

> **試験対策ポイント**: 「所有者と共同作成者の違いは？」→ **権限の付与ができるかどうか**です。RBAC は **Azure Resource Manager** に適用され、データレベルの権限ではないことも重要です。「最小権限の原則」に基づき、必要最小限のロールを割り当てるべきです。

---

### ゼロトラストモデル

ゼロトラストとは、**「何も信頼しない、常に検証する」** というセキュリティの考え方です。

#### 身近な例えで理解しよう

以前は「会社のビル内にいる人は全員信頼できる」（境界型セキュリティ）でした。ゼロトラストは「**たとえビル内にいても、毎回入館証を見せてもらう**」という考え方です。

#### ゼロトラストの 3 つの原則

| 原則 | 意味 |
|:---|:---|
| **明示的に検証する** | すべてのアクセスを毎回認証・認可する |
| **最小権限アクセス** | 必要最小限の権限のみ付与する（JIT / JEA） |
| **侵害を想定する** | 侵害は起こりうるものとして、被害を最小化する設計にする |

> **試験対策ポイント**: ゼロトラストの 3 原則は頻出です。「常に信頼しない」「最小権限」「侵害を想定」の 3 つをセットで覚えましょう。

---

### 多層防御（Defense in Depth）

多層防御とは、**複数のセキュリティ層を重ねて防御する戦略**です。1 つの層が突破されても、次の層で防ぎます。

#### 身近な例えで理解しよう

**城の防御**を想像してください。堀（第 1 層）→ 城壁（第 2 層）→ 見張り塔（第 3 層）→ 城門（第 4 層）と何重にも守られています。

#### 多層防御の 7 つの層

| 層（外側から順に） | 内容 | 例 |
|:---|:---|:---|
| **物理的セキュリティ** | データセンターの物理的保護 | 生体認証、監視カメラ |
| **ID とアクセス** | ユーザー認証とアクセス制御 | MFA、RBAC |
| **境界** | ネットワークの境界保護 | DDoS 対策、ファイアウォール |
| **ネットワーク** | ネットワーク内の通信制御 | NSG、サブネット分離 |
| **コンピューティング** | VM やコンテナの保護 | OS パッチ適用、マルウェア対策 |
| **アプリケーション** | アプリの脆弱性対策 | セキュアコーディング |
| **データ** | データの暗号化と保護 | 暗号化、アクセス制御 |

**ポイント**: データは多層防御の**最も内側（中心）** にあります。すべての層はデータを守るためにあります。

> **試験対策ポイント**: 「多層防御の中心にあるのは？」→ **データ** です。各層の役割と順番も出題されます。

---

### Microsoft Defender for Cloud

Microsoft Defender for Cloud は、**Azure やオンプレミス、他のクラウドのセキュリティ状態を監視し、脅威から保護するサービス**です。

#### 身近な例えで理解しよう

**家のホームセキュリティシステム**のようなものです。窓やドアのセンサーが異常を検知するとアラートを出し、「窓の鍵が開いています」（セキュリティの推奨事項）と教えてくれます。

#### 主な機能

| 機能 | 説明 |
|:---|:---|
| **セキュリティスコア** | セキュリティの強度を点数で評価（0～100%） |
| **推奨事項** | セキュリティを改善するための具体的なアドバイス |
| **脅威保護** | リアルタイムの脅威検出とアラート |
| **マルチクラウド対応** | Azure だけでなく AWS や GCP も監視可能 |

> **試験対策ポイント**: 「Azure のセキュリティ態勢を評価するサービスは？」→ **Microsoft Defender for Cloud** です。セキュリティスコアで現在の状態を可視化し、推奨事項で改善点を示してくれるサービスとして覚えましょう。

---

## 確認問題

### 問題 1
ある企業が、Azure に仮想マシンをデプロイする際、単一データセンターの障害からアプリケーションを保護したいと考えています。利用すべき機能は次のうちどれですか？

- A) リージョンペア
- B) 可用性ゾーン（Availability Zones）
- C) リソースグループ
- D) 管理グループ

---

### 問題 2
インターネットを経由せずに、専用のプライベート接続でオンプレミスネットワークと Azure を接続するサービスはどれですか？

- A) Azure VPN Gateway
- B) Azure Virtual Network
- C) Azure ExpressRoute
- D) Azure VNet ピアリング

---

### 問題 3
Azure Blob Storage で、ほとんどアクセスしないデータを最も低いストレージコストで保存するには、どのアクセス層を使用すべきですか？

- A) ホット
- B) クール
- C) コールド
- D) アーカイブ

---

## 解答・解説

### 問題 1 の解答: **B) 可用性ゾーン（Availability Zones）**

**解説**: 可用性ゾーンは、1 つのリージョン内にある物理的に独立したデータセンター群です。異なるゾーンにリソースを配置することで、**単一データセンターの障害**からアプリケーションを保護できます。リージョンペアは「リージョン全体の障害」に対する保護であり、単一データセンターの障害対策としては可用性ゾーンが適切です。

---

### 問題 2 の解答: **C) Azure ExpressRoute**

**解説**: Azure ExpressRoute は、**インターネットを経由しない専用のプライベート接続**を提供するサービスです。VPN Gateway もオンプレミスと Azure を接続しますが、インターネットを経由する暗号化接続です。問題文の「インターネットを経由せず」「専用のプライベート接続」がキーワードとなり、ExpressRoute が正解です。

---

### 問題 3 の解答: **D) アーカイブ**

**解説**: アーカイブ層は、**ストレージコストが最も安い**層です。ただし、アクセスコストは最も高く、データにアクセスするにはリハイドレート（ホットまたはクール層への移動）が必要で、数時間かかります。「ほとんどアクセスしない」「最も低いストレージコスト」というキーワードがアーカイブ層を指しています。
